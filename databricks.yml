bundle:
  name: finance_dlt_dev

resources:
  pipelines:
    bronze_pipeline:
      name: "Finance Bronze DLT - Dev"
      catalog: "finance_dev"
      schema: "bronze"
      libraries:
        - notebook:
            path: "notebooks/bronze_eventhub_ingest.py"
      storage: "abfss://lake@adbsdatalake.dfs.core.windows.net/dev/bronze"

    silver_pipeline:
      name: "Finance Silver DLT - Dev"
      catalog: "finance_dev"
      schema: "silver"
      libraries:
        - notebook:
            path: "notebooks/silver_trades_dlt.py"
      storage: "abfss://lake@adbsdatalake.dfs.core.windows.net/dev/silver"

    gold_pipeline:
      name: "Finance Gold DLT - Dev"
      catalog: "finance_dev"
      schema: "gold"
      libraries:
        - notebook:
            path: "notebooks/gold_trades_dlt.py"
      storage: "abfss://lake@adbsdatalake.dfs.core.windows.net/dev/gold"

targets:
  default:
    mode: development
    default: true

  dev:
    mode: development
    workspace:
      host: https://adb-4058806910823144.4.azuredatabricks.net
      root_path: /Workspace/Users/sreesatya2512@gmail.com/dev
